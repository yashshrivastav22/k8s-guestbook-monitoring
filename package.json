{
  "name": "k8s-guestbook-monitoring",
  "version": "1.0.0",
  "description": "Kubernetes Guestbook + Prometheus + Grafana via Pulumi",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "up": "pulumi up",
    "down": "pulumi destroy",
    "preview": "pulumi preview"
  },
  "dependencies": {
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/kubernetes": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
